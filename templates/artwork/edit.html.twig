{% extends 'base.html.twig' %}

{% block title %}Éditer {{artwork.title}}{% endblock %}

{% block body %}
    <h1 class="h2 text-center my-4">Éditer l'oeuvre :  {{artwork.title}}</h1>

    {{ include('artwork/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('admin_artwork_index') }}">back to list</a>

    {{ include('artwork/_delete_form.html.twig') }}
{% endblock %}
