{% extends 'adminBase.html.twig' %}

{% set query = app.request.query.get('q') %}

{% block title %}Gestion des utilisateurs{% endblock %}

{% block body %}
  {% include 'partials/flashes/error.html.twig' %}
  {% include 'partials/flashes/success.html.twig' %}
    <h1 class="h2 mt-4 mb-3 text-center">Gestion des utilisateurs</h1>
    <p class="text-center"><b>Nombre total d'utilisateur(s):</b> {{ users.getTotalItemCount }}</p>


    <div class="mb-4 text-center">
      <div class="d-inline-block">
          {# Affichage du menu permettant de naviguer entre les différentes pages #}
          {{ knp_pagination_render(users) }}
      </div>
    </div>

    <!-- Click on Modal Button -->
<button type="button" class="createArticleBtn btn altBtn mb-2" data-bs-toggle="modal" data-bs-target="#modalForm">
  <i class="fas fa-search me-2"></i>Rechercher
</button>

<!-- Modal -->
<div class="modal fade" id="modalForm" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
          <div class="modal-header">
              <h5 class="modal-title" id="exampleModalLabel">Rechercher un utilisateur</h5>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
          </div>
          <div class="modal-body">
              <form action="{{ path('admin_user_search') }}" method="GET">
                  <div class="mb-3">
                      <label class="form-label">Rechercher un utilisateur</label>
                      <input required type="text" class="form-control" id="article" name="q" placeholder="Rechercher...." />
                  </div>
                  <div class="modal-footer d-block">
                      <button type="submit" class="btn altBtn float-end">Rechercher</button>
                  </div>
              </form>
          </div>
      </div>
  </div>
</div>


    <div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead>
          <tr>
            <th class="text-center" scope="col">{{ knp_pagination_sortable(users, 'Id', 'a.id') }}</th>
            <th class="text-center" scope="col">{{ knp_pagination_sortable(users, 'Pseudonyme', 'a.pseudonym') }}</th>
            <th class="text-center" scope="col">{{ knp_pagination_sortable(users, 'Adresse email', 'a.email') }}</th>
            <th class="text-center" scope="col">{{ knp_pagination_sortable(users, 'Rôle', 'a.roles') }}</th>
            <th class="text-center" scope="col">{{ knp_pagination_sortable(users, 'Date d\'enregistrement', 'a.registrationDate') }}</th>
            <th scope="col">Actions</th>

          </tr>
        </thead>
        <tbody>
            {% for user in users %}
          <tr>
            <td class="text-center">{{ user.id }}</td>
            <td class="text-center"><b>{{ user.pseudonym|length > 40 ? user.pseudonym|striptags|slice(0, 40) ~ '…' : user.pseudonym|striptags  }}</b></td>
            <td class="text-center">{{ user.email|length > 40 ? user.email|striptags|slice(0, 40) ~ '…' : user.email|striptags  }}</td>
            <td class="text-center">{% if user.roles[0] == 'ROLE_ADMIN' %}Administrateur{% else %}Utilisateur{% endif %}</td>
            <td class="text-center">{{ user.registrationDate ? user.registrationDate|date('d/m/Y H:i:s') : '' }}</td>
            <td class="text-center">
                <div class="d-flex">
                    <a class="me-2" href="{{ path('user_show', {'slug': user.slug}) }}"><i class="far fa-eye"></i></a>
                    {% if user.roles[0] == 'ROLE_ADMIN' %}
                    {% else %}
                      <a id="deleteIcon" href="{{ path('admin_delete_account', {'slug': user.slug}) }}" onclick="return confirm('Souhaitez vous vraiment supprimer ce compte ?');"><i class="fas fa-trash me-2"></i></a>
                    {% endif %}
                </div>

            </td>
          </tr>
          {% else %}
          <tr>
              <td colspan="12" class="text-center">Aucune utilisateur trouvé</td>
          </tr>
      {% endfor %}
        </tbody>
      </table>
      </div>
      <div class="mt-5 mb-3 text-center">
        <div class="d-inline-block">
            {# Affichage du menu permettant de naviguer entre les différentes pages #}
            {{ knp_pagination_render(users) }}
        </div>
      </div>

      {% endblock %}
