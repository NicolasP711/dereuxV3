{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe{% endblock %}

{% block body %}


{% include 'partials/flashes/error.html.twig' %}
{% include 'partials/flashes/success.html.twig' %}

    <h1 class="h2 text-center mt-4">Modifier mon mot de passe</h1>

    <div class="offset-lg-3 col-lg-6 offset-lg-3 mt-5">
        {{ form_start(editPasswordForm) }}
        {{ form_errors(editPasswordForm) }}

        <div class="form-group mb-3">
            <label class="required" for="edit_password_form_currentpassword">{{editPasswordForm.currentpassword.vars.label}}</label>
            {{ form_errors(editPasswordForm.currentpassword) }}
            <div class="input-group mb-3">
                {{ form_widget(editPasswordForm.currentpassword) }}
                <span class="input-group-text" id="basic-addon1"><i id="togglePassword" class="fas fa-eye" onclick="toggleCurrentPasswordEditPassword()"></i></span>
            </div>
            {{ form_help(editPasswordForm.currentpassword) }}
        </div>

        <div class="form-group mb-3">
            <label class="required" for="edit_password_form_plainPassword_currentpassword">{{editPasswordForm.plainPassword.password.vars.label}}</label>
            {{ form_errors(editPasswordForm.plainPassword.password) }}
            <div class="input-group mb-3">
                {{ form_widget(editPasswordForm.plainPassword.password) }}
                <span class="input-group-text" id="basic-addon1"><i id="togglePassword1" class="fas fa-eye" onclick="toggleCurrentPassword3()"></i></span>
                {{ form_help(editPasswordForm.plainPassword.password) }}
            </div>
        </div>

        <div class="form-group mb-3">
            <label class="required" for="edit_password_form_plainPassword_confirm">{{editPasswordForm.plainPassword.confirm.vars.label}}</label>
            {{ form_errors(editPasswordForm.plainPassword.confirm) }}
            <div class="input-group mb-3">
                {{ form_widget(editPasswordForm.plainPassword.confirm) }}
                <span class="input-group-text" id="basic-addon1"><i id="togglePassword2" class="fas fa-eye" onclick="toggleCurrentPassword4()"></i></span>
            </div>
        </div>

            <div class="form-group mb-3">
                <button class="btn defaultBtn w-100">Modifier</button>
            </div>

        {{ form_end(editPasswordForm) }}
    </div>

    

{% endblock %}
